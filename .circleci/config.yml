version: 2
jobs:
  build:
    docker:
      - image: archlinux/base:latest

    steps:
      - checkout
      - run:
          name: Pyflakes
          command: pacman -Suuyy --noconfirm python-pyflakes ; pyflakes .